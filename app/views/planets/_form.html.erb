<%= form_with(model: planet) do |form| %>
  <% if planet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(planet.errors.count, "error") %> prohibited this planet from being saved:</h2>

      <ul>
        <% planet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :gravitational_accelaration %>
    <%= form.text_field :gravitational_accelaration %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :has_fuel %>
    <%= form.check_box :has_fuel %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
